# TDOLI — Cloudflare Pages redirects
# Règle unique : toutes les routes non trouvées → feed (SPA fallback)
# Les fichiers HTML statiques sont servis directement par Cloudflare Pages

/*    /tdoli-feed.html    200
